[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install uv", "uv sync --no-dev"]

[phases.build]
cmds = ["uv run alembic upgrade head"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
